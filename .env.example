# Client environment variables
VITE_APP_NAME=GuiltPing
VITE_GROUP_SIZE=5

# Web Push (optional)
VITE_PUSH_PUBLIC_KEY=REPLACE_WITH_VAPID_PUBLIC_KEY
VITE_PUSH_SERVER_URL=https://api.example.com/push/subscribe

# Display-only cron (actual scheduling happens on backend)
VITE_SUMMARY_CRON="0 20 * * *"
